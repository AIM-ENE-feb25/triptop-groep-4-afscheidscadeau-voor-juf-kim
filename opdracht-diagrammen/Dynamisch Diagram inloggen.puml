@startuml
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Dynamic.puml


title [Container] Triptop

Person(user, "Reiziger", "Beheert en wijzigt zijn reis")

System_Boundary(triptop, "n") {

    Container(web, "Triptop Web App", "React (JavaScript / react)", "Biedt gebruikers de mogelijkheid om hun reis te beheren en wijzigingen aan te brengen")

'    Nodig?
'    Container(api, "API Gateway", "Java MVC", "")

    Container(trip_manager, "Reisbeheer API-applicatie", "Java MVC", "Beheert reisoverzicht en wijzigingen van bouwstenen")

    ContainerDb(db, "Database", "PostgreSQL", "Opslag van reisdata, wijzigingen en gebruikersvoorkeuren")

    Container(authenticatie, "Authenticatie component", "Bevat functionaliteit voor het authenticeren van de gebruiker")

}

System_Ext(authService, "Authenticatie Service", "OAuth 2.0", "Regelt authenticatie via externe providers zoals Google")

Rel(user, web, "Gebruiker logt in")
Rel(web, trip_manager, "Verzend inloggegevens naar", JSON/HTTPS)

Rel(trip_manager, authenticatie, "Authenticeert via")
Rel(authenticatie, authService, "Authenticeert gebruiker als gebruiker via google inlogged")
Rel(authService, trip_manager, "Stuurt authenticatie token door naar")

Rel(authenticatie, db,"SELECT * FROM users WHERE username = ?", "SQL")
Rel(db, authenticatie, "Stuurt opgehaalde gebruikers data terug naar")
Rel(authenticatie, trip_manager, "Stuurt authenticatie token door naar")
Rel(trip_manager, web, "Stuurt authenticatie token door naar", "JSON/HTTP")



@enduml```


@enduml